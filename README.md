# Movie Emotion - Сервис подбора фильмов по эмоциональному запросу

Веб-сервис для кинолюбителей, который помогает подобрать фильмы на основе эмоционального запроса пользователя. Система анализирует эмоциональный профиль фильмов (радость, грусть, напряжение и т.д.) и рекомендует кино, соответствующее текущему настроению или желаемому эмоциональному запросу.

**Ссылка на рабочий проект:** https://emofilm.hshcode.ru/

## Основные возможности

- 🎬 **Каталог фильмов** с эмоциональным профилем каждого фильма
- 🔍 **Поиск по эмоциям** - найдите фильмы, которые вызывают нужные вам эмоции
- 📊 **Визуализация эмоций** - радиальная диаграмма эмоционального профиля фильма
- 🔔 **Система подписок** - подпишитесь на уведомления о новых фильмах с определенными эмоциями
- 📧 **Email уведомления** - получайте письма о новых фильмах по вашим подпискам
- 👤 **Личный кабинет** - управляйте избранными фильмами, подписками и настройками
- 🔐 **Авторизация и регистрация** - полная система пользователей

## Технологии

* **Backend**: Python 3.11+, Django 5.2
* **Database**: PostgreSQL
* **API**: Django REST Framework
* **Frontend**: Bootstrap 5, Chart.js (для визуализации эмоций)
* **Email**: SMTP (настроен через переменные окружения)
* **Deployment**: Docker, Docker Compose

## Установка и запуск проекта локально

### Предварительные требования

- Python 3.11 или выше
- PostgreSQL
- Git

### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/ваш-юзернейм/movie-emotion.git
cd movie-emotion
```

### Шаг 2: Установка зависимостей

Для установки зависимостей:
```bash
uv pip compile -q pyproject.toml -o requirements.txt && \
uv pip install --no-deps -r requirements.txt
```

Можно использовать poetry:
```bash
poetry install --no-root
```

Если нужно зафиксировать новые зависимости 

```bash
uv lock
```

### Шаг 3: Настройка переменных окружения

Создайте файл `.env` и скопируйте в него содержимое `env.example`, заполните реальными данными.

### Шаг 4: Создание базы данных

```bash
# Создайте базу данных PostgreSQL
createdb movie_emotion

# Выполните миграции
python manage.py migrate
```

### Шаг 5: Загрузка начальных данных

```bash
python manage.py load_initial_data
```

Эта команда создаст:
- Суперпользователя (admin)
- Базовые эмоции
- ~30 тестовых фильмов с эмоциональными оценками

### Шаг 6: Запуск сервера разработки

```bash
python manage.py runserver
```

### Шаг 7: Открытие проекта в браузере

Перейдите по ссылке: localhost:8000/ (предварительно нужно открыть порты в docker-compose.yml)

## Запуск через Docker

### Шаг 1: Создайте файл .env (см. выше)

### Шаг 2: Запустите контейнеры

```bash
docker-compose up -d
```

### Шаг 3: Выполните миграции и загрузите данные

```bash
docker-compose exec django-service python manage.py migrate
docker-compose exec django-service python manage.py load_initial_data
```

## Структура проекта

```
movie-emotion/
├── films/              # Приложение для работы с фильмами
├── emotions/           # Приложение для работы с эмоциями
├── users/              # Приложение для работы с пользователями
├── notifications/      # Приложение для уведомлений и подписок
├── movie_emotion/      # Основные настройки проекта
├── templates/          # HTML шаблоны
├── staticfiles/        # Статические файлы (CSS, JS)
├── media/              # Медиа файлы (постеры, аватары)
├── manage.py           # Django management script
├── pyproject.toml    # Зависимости проекта
├── docker-compose.yml  # Docker Compose конфигурация
└── README.md           # Этот файл
```

## API Endpoints

Проект включает REST API для работы с фильмами и эмоциями:

- `GET /api/films/` - Список фильмов
- `GET /api/films/{id}/` - Детали фильма
- `GET /api/films/by_emotion/?emotion_ids=1,2&min_intensity=7` - Фильмы по эмоциям
- `GET /api/films/{id}/emotion_profile/` - Эмоциональный профиль фильма
- `GET /api/emotions/` - Список эмоций

## Основные модели данных

1. **Film** - Фильм с информацией о названии, году, режиссере и т.д.
2. **Emotion** - Эмоция (радость, грусть, страх и т.д.)
3. **FilmEmotionRating** - Оценка интенсивности эмоции в фильме (1-10)
4. **UserProfile** - Профиль пользователя с настройками
5. **Subscription** - Подписка пользователя на уведомления по эмоциям
6. **Notification** - Уведомление для пользователя

## Функционал

### Для гостей:
- Просмотр каталога фильмов
- Поиск фильмов по названию, жанру, году
- Поиск фильмов по эмоциям
- Просмотр детальной информации о фильме
- Просмотр эмоционального профиля фильма (радиальная диаграмма)

### Для зарегистрированных пользователей:
- Все возможности гостя
- Регистрация и авторизация
- Личный кабинет
- Добавление фильмов в избранное
- Создание подписок на эмоции
- Получение уведомлений о новых фильмах (на сайте и по email)
- Настройка профиля (аватар, предпочитаемые эмоции, частота уведомлений)

### Для администраторов:
- Полное управление фильмами через админ-панель
- Управление эмоциями
- Управление пользователями
- Просмотр подписок и уведомлений

## Система уведомлений

При создании нового фильма система автоматически:
1. Анализирует эмоциональный профиль фильма
2. Находит всех пользователей с активными подписками на соответствующие эмоции
3. Создает уведомление в базе данных
4. Отправляет email уведомление (если включено в настройках пользователя)

## Разработка

### Создание миграций

```bash
python manage.py makemigrations
python manage.py migrate
```

### Создание суперпользователя

```bash
python manage.py createsuperuser
```

### Сбор статических файлов

```bash
python manage.py collectstatic
```

## Автор

Проект разработан в рамках учебного курса по технологиям программирования на Python. Разработал Черемисин Максим Ильич
